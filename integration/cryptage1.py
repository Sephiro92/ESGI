# coding: utf-8

import random   #import de la fonction "random"     #les commentaires foutent la merde

crypt = raw_input("votre message : ")   #récupération d'une saisie en 'brut' et ensuite mis dans la variable "crypt"""" 
#print len(crypt)       #len(var) affiche la longueur de la saisie en nombre
#print(crypt[n]),       #affiche le caractère à la position 'n' de 'crypt' et la virgule permet de ne pas mettre le retour chariot
#print(encrypt),        #affiche le contenu de la variable 'encrypt'
n = 0
rdm = 0                 #si le genre de la variable n'est pas précisé il prendra certaine valeur par défaut pour en déduire le genre
encrypt = [] 
rdm = random.randint(0, 5)
while n < len(crypt) :

    if crypt[n] == "a" :
        rdm = random.randint(6, 9)
        encrypt.append(rdm)

    if crypt[n] == "b" :
        rdm = random.randint(10, 13)
        encrypt.append(rdm)

    if crypt[n] == "c" :
        rdm = random.randint(14, 17)
        encrypt.append(rdm)
        
    if crypt[n] == "d" :
        rdm = random.randint(18, 21)
        encrypt.append(rdm)
        
    if crypt[n] == "e" :
        rdm = random.randint(22, 25)
        encrypt.append(rdm)
        
    if crypt[n] == "f" :
        rdm = random.randint(26, 29)
        encrypt.append(rdm)
        
    if crypt[n] == "g" :
        rdm = random.randint(30, 33)
        encrypt.append(rdm)
        
    if crypt[n] == "h" :
        rdm = random.randint(34, 37)
        encrypt.append(rdm)
        
    if crypt[n] == "i" :
        rdm = random.randint(38, 41)
        encrypt.append(rdm)
        
    if crypt[n] == "j" :
        rdm = random.randint(42, 45)
        encrypt.append(rdm)
        
    if crypt[n] == "k" :
        rdm = random.randint(46, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "l" :
        rdm = random.randint(50, 53)
        encrypt.append(rdm)
        
    if crypt[n] == "m" :
        rdm = random.randint(54, 57)
        encrypt.append(rdm)
        
    if crypt[n] == "n" :
        rdm = random.randint(58, 61)
        encrypt.append(rdm)
        
    if crypt[n] == "o" :
        rdm = random.randint(62, 65)
        encrypt.append(rdm)
        
    if crypt[n] == "p" :
        rdm = random.randint(66, 69)
        encrypt.append(rdm)
        
    if crypt[n] == "q" :
        rdm = random.randint(70, 73)
        encrypt.append(rdm)
        
    if crypt[n] == "r" :
        rdm = random.randint(74, 77)
        encrypt.append(rdm)
        
    if crypt[n] == "s" :
        rdm = random.randint(78, 81)
        encrypt.append(rdm)
        
    if crypt[n] == "t" :
        rdm = random.randint(82, 85)
        encrypt.append(rdm)
        
    if crypt[n] == "u" :
        rdm = random.randint(86, 89)
        encrypt.append(rdm)
        
    if crypt[n] == "v" :
        rdm = random.randint(90, 93)
        encrypt.append(rdm)
        
    if crypt[n] == "w" :
        rdm = random.randint(94, 97)
        encrypt.append(rdm)
        
    if crypt[n] == "x" :
        rdm = random.randint(100, 103)
        encrypt.append(rdm)
        
    if crypt[n] == "y" :
        rdm = random.randint(104, 107)
        encrypt.append(rdm)
        
    if crypt[n] == "z" :
        rdm = random.randint(108, 111)
        encrypt.append(rdm)
        
    if crypt[n] == "A" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "B" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "C" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "D" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "E" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "F" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "G" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "H" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "I" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "J" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "K" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "L" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "M" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "N" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "O" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "P" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "Q" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "R" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "S" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "T" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "U" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "V" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "W" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "X" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "Y" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
    if crypt[n] == "Z" :
        rdm = random.randint(40, 49)
        encrypt.append(rdm)
        
     
    n = n + 1

#print(encrypt)

for lettre in encrypt:
     print(lettre),
